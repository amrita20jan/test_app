<%= form_for(Comment.new) do |f| %>
  
  <div class="field" >
    <%= f.label :comments %><br>
    <%= f.text_area :description %>
   <%= f.hidden_field :event_id, :value => @event.id %>
  </div>
  
  <div class="actions">
    <%= button_to "Create comment" ,:action=>"create", :controller=>"comments"  %>
  </div>