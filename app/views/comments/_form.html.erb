<%= form_for(Comment.new) do |f| %>
  
  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_area :description %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
